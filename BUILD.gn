static_library("electron_hooks") {
    sources = [
        "interceptors.cc",
        "interceptors.h",
    ]
    deps = [
        "//base",
    ]
    include_dirs = [ "/usr/local/include" ]
    lib_dirs = [ "/usr/local/lib64", "/usr/local/lib" ]
    libs = [ "cmark" ]
}
